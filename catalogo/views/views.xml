<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="catalogo.list">
      <field name="name">catalogo list</field>
      <field name="model">catalogo.catalogo</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="catalogo.action_window">
      <field name="name">catalogo window</field>
      <field name="res_model">catalogo.catalogo</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="catalogo.action_server">
      <field name="name">catalogo server</field>
      <field name="model_id" ref="model_catalogo_catalogo"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="catalogo" id="catalogo.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="catalogo.menu_1" parent="catalogo.menu_root"/>
    <menuitem name="Menu 2" id="catalogo.menu_2" parent="catalogo.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="catalogo.menu_1_list" parent="catalogo.menu_1"
              action="catalogo.action_window"/>
    <menuitem name="Server to list" id="catalogo" parent="catalogo.menu_2"
              action="catalogo.action_server"/>
    -->
  </data>
</odoo>